# The libVIIPER backend presents the virtual DualSense as a real USB device
# through USB/IP, which needs the in-kernel client. Without this the module is
# absent after a reboot and the backend fails with a message about needing root.
vhci-hcd
